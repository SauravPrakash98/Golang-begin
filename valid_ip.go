package main

/*
import "fmt"

func cnum(s string) int {
	var x int
	x = (int(s[0]-'0'))*100 + (int(s[1]-'0'))*10 + (int(s[2] - '0'))
	return x
}

func main() {
	var str string
	str = "172.168.192.0011"
	//	fmt.Println(len(str))
	var k int
	for i := 0; i < len(str); i++ {
		if str[i] == '.' || i == len(str)-1 {
			//fmt.Printf("%d\n", i)
			var s string
			var num int
			if i == len(str)-1 {
				s = str[k : i+1]

			} else {
				s = str[k:i]
			}
			if len(s) < 4 {
				num = cnum(s)
				//	fmt.Println(num)
				if num > 256 {
					fmt.Println("Oh no ! ")
				}
			} else {
				fmt.Println("Oh no ! ")
			}

			if i != len(str)-1 {
				k = i + 1
			}
			//		fmt.Println(s)
		}
	}
}
*/
